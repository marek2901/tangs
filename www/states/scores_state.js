var scores_state = function () {
    console.log("Scores Initialized");
}

scores_state.prototype = {
    preload: function () {
        // load assets and stuff neccessary to display later
    },
    create: function () {
        // Runs once, after preload
    }
}
